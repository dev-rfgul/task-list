<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>File Details</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Tailwind CSS CDN -->
  </head>
  <body
    class="bg-gradient-to-b from-zinc-900 to-zinc-800 flex items-center justify-center min-h-screen"
  >
    <div
      class="bg-zinc-800 p-8 rounded-lg shadow-lg max-w-lg w-full text-center transform hover:scale-105 transition-transform duration-300 ease-in-out"
    >
      <div class="flex justify-end">
        <a
          href="/delete?filename=<%= filename %>"
          class="bg-red-600 w-8 h-8 rounded-full text-white text-xl font-bold flex items-center justify-center cursor-pointer hover:bg-red-700"
          onclick="return confirm('Are you sure you want to delete this file?');"
        >
          X
        </a>
      </div>

      <h1 class="text-4xl font-extrabold text-gray-100 mb-6">
        <%= filename %>
      </h1>
      <p class="text-gray-300 leading-relaxed mb-8 text-lg"><%= filedata %></p>
      <a
        href="/"
        class="inline-block bg-blue-600 text-white px-8 py-3 rounded-lg shadow-lg hover:bg-blue-700 transition duration-300"
      >
        Go back to home
      </a>
    </div>
  </body>
</html>
